services:
  monprojo:
    image: ghcr.io/gudupont/monprojo:latest
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "file:/app/data/monprojo.db"
      TMDB_API_KEY: "${TMDB_API_KEY}"
      OMDB_API_KEY: "${OMDB_API_KEY}"
      SESSION_SECRET: "${SESSION_SECRET}"
      APP_PASSWORD_HASH: "${APP_PASSWORD_HASH}"
    volumes:
      - monprojo-data:/app/data
    restart: unless-stopped

volumes:
  monprojo-data:
